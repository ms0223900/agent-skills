---
name: adjust
description: 用於補充調整既有功能：先更新 US、再補測試、再實作、跑測試直到全過、最後驗收 US。使用時機：使用者提出補充需求或功能調整，例如「補一下 XXX」、「調整 XXX 的行為」、「這邊需要多支援 YYY」。
---

# 補充調整工作流程（Adjust Workflow）

## 目標

當使用者提出補充需求或功能調整時，依序完成：**US 更新 → 測試補充 → 功能實作 → 測試通過 → 驗收 US**。

---

## 執行流程

### Step 1：找出對應 US 目錄

1. 讀取目前的 git branch 名稱（`git branch --show-current`）。
2. 從 branch 名稱中擷取 ticket ID，例如 `feature/SPRD-965` → `SPRD-965`。
3. 前往 `docs/specs/{TICKET_ID}/us/` 列出所有 US 檔案。
4. 根據使用者描述的調整內容，判斷：
   - 哪些既有 US 需要修改（調整驗收條件或描述）
   - 是否需要新增一份 US（若調整範圍超出現有 US 涵蓋）

---

### Step 2：更新或新增 US

依照以下原則調整 US，與 `/user-stories` skill 保持一致：

- **調整既有 US**：只補充或修改受影響的驗收條件，不大改未受影響的部分。
- **新增 US**：若需求超出現有 US，依照 `US-0X` 序號新增一份，格式如下：

```markdown
### US-0X：{標題}

**作為** {角色}
**我想要** {功能}
**以便** {目的}

---

**輸入格式**：...

**輸出格式**：...

**驗收條件**：
- [ ] ...

**依賴關係**：...

**優先級**：P0 / P1 / P2
**相關功能**：...
```

- 存檔路徑：`docs/specs/{TICKET_ID}/us/US-0X-{slug}.md`
- 命名序號接續現有最大編號。

---

### Step 3：補充或新增測試

1. **尋找現有測試檔**：搜尋與被修改功能相關的 `*.test.ts` / `*.spec.ts`。
   - 若存在 → 在現有測試檔中補充對應的 test case。
   - 若不存在 → 在功能檔案旁新建單元測試檔（`{filename}.test.ts`）。
2. **測試範圍原則**：
   - 只測試本次調整涉及的功能邏輯。
   - 以「正向行為」為主，不要過度測試錯誤邊界或各種極端情境（除非 US 驗收條件明確要求）。
   - 測試命名清楚反映 US 驗收條件。

---

### Step 4：實作功能

依照 `/feature` skill 的實作原則進行：

- Small 變更：直接說明方案後實作。
- Medium / Large：先盤點影響範圍，必要時切換 Plan 模式。
- 嚴格遵守專案規範（Vue 2 Options API、現有 API 封裝、樣式變數等）。
- 優先沿用現有 pattern，不引入新的抽象。

---

### Step 5：跑測試直到全部通過

```bash
npx jest {測試檔路徑} --no-coverage
```

- 若有失敗，診斷原因並修正（程式碼或測試），直到全部通過。
- 不要為了讓測試通過而降低測試覆蓋範圍或跳過斷言。

---

### Step 6：驗收 US

執行 `/us-acceptance-check`，對 Step 1 中判定受影響的所有 US 進行驗收，確認驗收條件清單與驗收說明皆已更新。

---

## 注意事項

- US 一律存放於 `docs/specs/{TICKET_ID}/us/`，不要放到其他位置。
- 測試只確保功能範圍內的行為正確，不做「防禦性全面覆蓋」。
- 若評估後發現調整幅度過大，暫停實作，先與使用者確認切分範圍。
